# This project uses `uv` (see CLAUDE.md). Run `uv sync` to install.
# This file is kept as a fallback for environments that cannot run uv.
# Authoritative dependency list lives in pyproject.toml after session 0.2.

# Core CV
ultralytics>=8.3.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
pillow>=10.0.0
scipy>=1.11.0
filterpy>=1.4.5
scikit-learn>=1.3.0
pyyaml>=6.0

# Pose biomech backend (RTMPose via ONNX — avoids mmcv/mmpose build hell)
onnxruntime>=1.25.0

# UI
streamlit>=1.28.0
plotly>=5.17.0
pandas>=2.1.0
matplotlib>=3.7.0
seaborn>=0.13.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-multipart>=0.0.9

# Background jobs
rq>=1.16.0
redis>=5.0.0

# AI summary
openai>=1.50.0
python-dotenv>=1.0.0

# Dev
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.4.0
